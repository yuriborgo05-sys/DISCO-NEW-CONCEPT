// Firebase Configuration - Placeholder for Production
// To activate, insert your Firebase project credentials here.

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "nox-pwa.firebaseapp.com",
  projectId: "nox-pwa",
  storageBucket: "nox-pwa.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// For now, we remain in DEMO MODE using our local services.
export const isProduction = false;
export default firebaseConfig;
